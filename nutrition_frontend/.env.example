# Example environment variables for the Nutrition Frontend (Create React App)
# Copy this file to `.env` and fill in values.

# Frontend (browser) environment
# Use your Supabase project's API URL and the anon public key for client-side usage.
REACT_APP_SUPABASE_URL=your_supabase_api_url
REACT_APP_SUPABASE_KEY=your_supabase_anon_public_key

# Optional (Node-only) environment for scripts like scripts/seedSupabase.js:
# If SUPABASE_URL/SUPABASE_KEY are not set, the script will fall back to the CRA-prefixed ones above.
# SUPABASE_URL=your_supabase_api_url
# SUPABASE_KEY=your_supabase_service_or_anon_key
